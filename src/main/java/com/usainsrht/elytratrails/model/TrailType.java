package com.usainsrht.elytratrails.model;

/**
 * The type of particle behaviour a trail uses.
 */
public enum TrailType {
    /** Single, unchanging particle effect. */
    STATIC,
    /** Cycling colours / shifting patterns. */
    ANIMATED,
    /** Geometric shapes (spirals, wings, etc.). */
    COMPLEX
}

